[package]
name = "joshgit777/omega"
version = "0.1.5"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["build", "src"]
include = ["src/shared/Omega"]

descriptions = "NPC System based on character controllers and state machines"

[dependencies]
